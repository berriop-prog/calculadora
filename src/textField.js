import React from 'react';

const TextField = (props) => {
    return (
        <div>
            <h1>Felicitaciones</h1>
            <p>{props.nombre} es una pro</p>
        </div>
    );
}

export default TextField;